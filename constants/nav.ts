// Navigation constants - icons are imported via PharosIcon wrapper
export type Tab = 'Dashboard' | 'Simulator' | 'Insights' | 'Advisor';

// Note: Icons are imported directly in AppShell.tsx to comply with lint guards
export const NAV_LABELS: Tab[] = ['Dashboard', 'Simulator', 'Insights', 'Advisor'];
