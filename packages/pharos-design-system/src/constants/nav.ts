// Navigation constants - icons are imported via PharosIcon wrapper
export type Tab = 'Dashboard' | 'Position' | 'Recommendations' | 'Execute' | 'Trial';

// Note: Icons are imported directly in AppShell.tsx to comply with lint guards
export const NAV_LABELS: Tab[] = ['Dashboard', 'Position', 'Recommendations', 'Execute', 'Trial'];
